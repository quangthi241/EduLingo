export default function PlacementIntroPage() {
  return (
    <main className="mx-auto flex w-full max-w-xl flex-col gap-4 px-6 py-16">
      <h1 className="font-[family-name:var(--font-literata)] text-3xl font-semibold tracking-tight">
        Placement
      </h1>
      <p className="text-[color:var(--color-ink-muted)]">
        Placement flow lands here — downstream spec #1.
      </p>
    </main>
  );
}
